# 40 Day Campaign

Independent static site for 40DayCampaign.com, generated from the LifeTogether master library. The deployment-ready folder contains its own application, site-specific source index, licensed fonts, metadata, and Netlify fallback routing.

Regenerate from the repository root with the network build script so title, subtitle, duration, and source-status changes remain synchronized with the master library.
