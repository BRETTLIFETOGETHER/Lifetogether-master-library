# Christian Advisor Network

Independent static site for ChristianAdvisorNetwork.com, generated from the LifeTogether master library. The deployment-ready folder contains its own application, site-specific source index, licensed fonts, metadata, and Netlify fallback routing.

Regenerate from the repository root with the network build script so title, subtitle, duration, and source-status changes remain synchronized with the master library.
